# Built binary
/dominion

# Local env (PIN etc.)
env

# Logs
*.log

# Node dev tooling (JS tests only)
node_modules/

# Client app build projects (generated)
apps/android/
apps/android-overlay/res-raw/

# Built client binaries (keep the READMEs, ignore the outputs)
/client_app/*
!/client_app/README.md
*.apk
*.AppImage
*.AppImage.zsync
# Distributable Android build is committed; the AppImage exceeds the 100 MiB
# git limit and is attached to a GitHub release instead (see apps/release.sh).
!/client_app/dominion_0.1.apk
apps/dist/
apps/android/app/build/
apps/android/.gradle/
apps/android/local.properties
apps/*/builder-debug.yml
linux-unpacked/

# Branding override (kept local)
assets/override_logo.*

# Third-party / personal icon material (not redistributable)
apps/icons/terran*
